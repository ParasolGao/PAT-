#include <iostream>
#include <algorithm>
using namespace std;

bool cmp(const int a,const int b){
	return a > b;
}
int main(int argc, char const *argv[])
{
	int k;
	int a[102],res[101];

	cin >> k;
	for (int i = 0; i < k; i++)
	{
		cin >> a[i];
	}
	sort(a,a+k,cmp);
	for (int i = 0; i < k; i++){

	}
	return 0;
}
